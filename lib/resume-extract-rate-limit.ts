/**
 * @deprecated Import from `@/lib/resume-ai-rate-limit` — Extract and Generate
 * share one pool.
 */
export {
    enforceResumeAiRateLimit as enforceResumeExtractRateLimit,
    rateLimitResponseHeaders
} from "@/lib/resume-ai-rate-limit";

